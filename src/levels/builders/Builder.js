


// each builder should include a build function

/**
 * This fills the tileMap with the builder tiles for the rgb
 * and ensure connectively to the includeLocs
 * @param tileMap
 * @param rgb
 * @param includeLocs
 */
function build(tileMap, rgb, includeLocs) {

}
